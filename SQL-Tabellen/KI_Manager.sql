CREATE TABLE KI_Manager (
      KI_ID INTEGER PRIMARY KEY,
      Aufgabe VARCHAR(100),
      Erstellungsdatum DATE,
      Ergebnis TEXT,
      CO2_Emission DECIMAL(10,2),
      KI_Spar DECIMAL(10,2),
      SensorDaten VARCHAR(255)
                 );


